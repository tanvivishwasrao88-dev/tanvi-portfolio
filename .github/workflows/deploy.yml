name: Deploy Tanvi Website

on:
 push:
  branches: [main]

jobs:
 build:
  runs-on: ubuntu-latest

  steps:
  - name: Checkout code
    uses: actions/checkout@v3

  - name: Build Website
    run: echo "Building Tanvi Portfolio"

  - name: Deploy Website
    run: echo "Website deployed successfully"
